rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any authenticated user to read/write for testing purposes
    // We'll tighten this up later
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
